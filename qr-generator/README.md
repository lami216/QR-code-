# QR Studio application

The Next.js application for the QR Studio browser-based static QR generator. Run `npm install`, `npm run lint`, and `npm run build` from this directory. Configure `NEXT_PUBLIC_SITE_URL` for production. See the repository-level README and `docs/` for architecture, advertising variables, SEO decisions and validation limitations.
